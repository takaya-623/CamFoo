<% content_for(:title, 'ログイン | CamFoo') %>
<div class="login-image">
  <div class="bg-rgba">
    <h2>ログイン</h2>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col-10 offset-1 m-auto text-center">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <div class="form-group">
            <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "メールアドレス", class: "w-50 text-center" %>
          </div>
        </div>
        <div class="field">
          <div class="form-group">
            <%= f.password_field :password, autocomplete: "current-password", placeholder: "パスワード", class: "w-50 text-center" %>
          </div>
        </div>
        <% if devise_mapping.rememberable? %>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label :ログイン状態を保存する %>
          </div>
        <% end %>

        <div class="actions">
          <%= f.submit "ログインする", class: "w-50" %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
